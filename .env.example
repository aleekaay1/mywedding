# Public site URL (no trailing slash)
VITE_SITE_URL=https://hamarishaadi.vercel.app

# Admin panel password (https://yoursite.com/admin)
ADMIN_PASSWORD=wedding2026

# Required on Vercel so uploaded guest lists persist for invite links
# Vercel Dashboard → Storage → Blob → create store → copy token
BLOB_READ_WRITE_TOKEN=
